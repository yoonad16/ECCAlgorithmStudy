#include <iostream>
using namespace std;

long long n;
long long count = 0;

int main(){
    ios:: sync_with_stdio(0);
    cin.tie(0);
    
    cin >> n;
    count = n*n;
    
    cout << count << '\n';
    cout << 2;
}